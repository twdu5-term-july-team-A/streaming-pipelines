version: "3"
services:
  e2e-test:
    build:
      context: .
      dockerfile: Dockerfile
    networks:
      - streamingdatapipeline_streaming-data-internal
    restart: on-failure

networks:
  streamingdatapipeline_streaming-data-internal:
    external: true